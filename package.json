{
  "name": "ebac-exerc-22",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "cypress:open": "npx cypress open",
    "cypress:run": "npx cypress run",
    "cypress:run:junit": "npx cypress run --reporter junit --reporter-options \"mochaFile=cypress/reports/junit/results-[hash].xml,toConsole=true\"",
    "cypress:record": "npx cypress run --record",
    "generate:report": "node scripts/generate-cypress-report.js",
    "test": "npm run cypress:run"
  },
  "devDependencies": {
    "@reportportal/agent-js-cypress": "^5.5.3",
    "cypress": "^13.0.0"
  }
}
